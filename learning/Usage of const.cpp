#include <iostream>
using namespace std;
int main()
{
    int a = 10;
    double b = 20.5;
    // int *const p=&a;
    // p=&b;
    // const在标识符前面，就代表着类型被定死，不可更改
    // void const *p=&a;
    // 如果const在类型前面，可以随意指向内容，但是不可以利用该指针修改指向的内容。
}